# P1-002 implementation record

P1-002 defines the five canonical Access Profile v2 modes in a shared JSON catalog and schema, plus Dart and Python domain models. Both languages round-trip all profiles and reject the same shared invalid-policy vectors. Profiles remain authority ceilings rather than effect grants.
