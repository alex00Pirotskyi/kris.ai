# P1-004 implementation record

The deterministic policy engine resolves Access Profile v2, registered capabilities, overlays, approvals, explicit widening and effect targets into an allow, deny or approval-required decision. Only allow produces a bounded Capability Grant v2 draft.
