# P1-003 implementation record

Capability Grant v2 binds every worker effect to the authenticated invocation and rejects modified, expired, replayed, exhausted and mismatched grants before effect dispatch.
