name: P3-006B Controlled Upload Staging Materializer

on:
  push:
    branches:
      - 'agent/product/p3-006b-upload-staging'
    paths:
      - '.github/workflows/p3-006b-materialize.yml'

permissions:
  contents: write

jobs:
  materialize:
    name: build-test-and-publish-upload-staging
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      BASE_COMMIT: c831851f604d404b1d55c6bc204949a1813cf155
      TARGET_BRANCH: agent/product/p3-006b-upload-staging
      ENCODED_SHA256: bca9374466205bf8ce615bdea36e7f1c7f1bda9b6c0dd4322e86d54e986be76f
      PATCH_SHA256: 1931d656f25dd7ba186b8eba0423565fb6ba6af741c8a8f899100bb3cf66bc8c
    steps:
      - name: Check out exact transport head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: true
      - name: Install exact Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.0.0
        with:
          node-version: '24.18.0'
          check-latest: false
          cache: npm
          cache-dependency-path: automation_host/package-lock.json
      - name: Install exact Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
        with:
          flutter-version: '3.44.8'
          channel: stable
          cache: true
      - name: Materialize exact candidate
        shell: bash
        run: |
          set -euo pipefail
          cat .github/p3-006b-patch/* > "$RUNNER_TEMP/p3-006b.patch.gz.b64"
          printf '%s  %s\n' "$ENCODED_SHA256" "$RUNNER_TEMP/p3-006b.patch.gz.b64" | sha256sum --check --strict
          base64 --decode "$RUNNER_TEMP/p3-006b.patch.gz.b64" > "$RUNNER_TEMP/p3-006b.patch.gz"
          gzip --decompress --stdout "$RUNNER_TEMP/p3-006b.patch.gz" > "$RUNNER_TEMP/p3-006b.patch"
          printf '%s  %s\n' "$PATCH_SHA256" "$RUNNER_TEMP/p3-006b.patch" | sha256sum --check --strict
          git apply --index "$RUNNER_TEMP/p3-006b.patch"
          rm -rf .github/p3-006b-patch .github/workflows/p3-006b-materialize.yml
      - name: Verify automation host
        shell: bash
        run: |
          set -euo pipefail
          node --check automation_host/src/browser-runtime.mjs
          npm ci --prefix automation_host --ignore-scripts --no-audit --no-fund
          npm test --prefix automation_host
          git diff --exit-code -- automation_host/package.json automation_host/package-lock.json
      - name: Verify Flutter product contracts
        shell: bash
        run: |
          set -euo pipefail
          flutter pub get
          dart format \
            lib/product/browser/browser_runtime.dart \
            test/product/browser/browser_runtime_process_test.dart
          dart format --output=none --set-exit-if-changed \
            lib/product/browser/browser_runtime.dart \
            test/product/browser/browser_runtime_process_test.dart
          flutter test test/product/browser
          flutter analyze --fatal-infos --fatal-warnings
      - name: Enforce exact product scope
        shell: bash
        run: |
          set -euo pipefail
          git add -A
          git diff --cached --check
          python3 - <<'PY'
          import subprocess

          base = 'c831851f604d404b1d55c6bc204949a1813cf155'
          expected = {
              'automation_host/src/browser-runtime.mjs',
              'automation_host/src/browser-runtime.test.mjs',
              'lib/product/browser/browser_runtime.dart',
              'test/product/browser/browser_runtime_process_test.dart',
          }
          actual = set(
              subprocess.check_output(
                  ['git', 'diff', base, '--name-only', '--'], text=True
              ).splitlines()
          )
          if actual != expected:
              raise SystemExit(
                  f'P3-006B product scope mismatch '
                  f'actual={sorted(actual)} expected={sorted(expected)}'
              )
          PY
      - name: Publish exact verified source candidate
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'feat(p3-006b): add controlled upload staging'
          git push origin "HEAD:refs/heads/$TARGET_BRANCH"
