name: TEMP PR14 Standalone Transports Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain retirement
        shell: bash
        run: |
          echo 'Standalone PR14 push, pull-request-target, workflow-run, and issue-comment transports are retired.'
          echo 'The exact dependent job inside product-gates is the active recovery handoff.'
          echo 'Human roadmap authority: docs/roadmap/MASTER.md'
