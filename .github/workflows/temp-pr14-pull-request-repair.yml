name: TEMP PR14 Broad Workflow Run Repair Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain retirement
        shell: bash
        run: |
          echo 'This broad workflow_run listener is retired.'
          echo 'The exact run-id and run-attempt pinned successor is authoritative.'
          echo 'Roadmap authority: docs/roadmap/MASTER.md'
