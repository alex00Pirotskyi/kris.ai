name: P5-010 command palette finalizer

on:
  pull_request:
    branches:
      - main
    paths:
      - .repo-p5-010-finalize.py
      - .repo-p5-010-compat-fix.py
      - .github/workflows/p5-010-command-palette-finalize.yml

permissions:
  contents: write

env:
  BASE_MAIN_SHA: 34db9b60755ae6150483fff9d5ebbe3a44561dd7
  TARGET_BRANCH: agent/p5-010-command-palette-20260823
  PATCH_PATH: .repo-p5-010-finalize.py
  FIX_PATH: .repo-p5-010-compat-fix.py
  FINALIZER_PATH: .github/workflows/p5-010-command-palette-finalize.yml
  SOURCE_SHA: ${{ github.event.pull_request.head.sha }}

jobs:
  finalize:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ env.SOURCE_SHA }}
          fetch-depth: 0
          persist-credentials: true

      - name: Verify exact P5-010 transport
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin main "$TARGET_BRANCH"
          test "$(git rev-parse HEAD)" = "$SOURCE_SHA"
          test "$(git rev-parse origin/main)" = "$BASE_MAIN_SHA"
          test "$(git merge-base HEAD origin/main)" = "$BASE_MAIN_SHA"
          test "$(git rev-parse origin/$TARGET_BRANCH)" = "$SOURCE_SHA"
          test -f "$PATCH_PATH"
          test -f "$FIX_PATH"
          test -f "$FINALIZER_PATH"
          test -z "$(git status --short)"

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.12.10'

      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: '3.44.8'
          channel: stable
          cache: true
          pub-cache: true

      - name: Apply P5-010 and remove transport
        shell: bash
        run: |
          set -euo pipefail
          flutter pub get
          python -m py_compile "$PATCH_PATH" "$FIX_PATH"
          python "$PATCH_PATH"
          python "$FIX_PATH"
          git rm -f "$PATCH_PATH" "$FIX_PATH" "$FINALIZER_PATH"
          dart format \
            lib/product/p5_command_palette.dart \
            lib/product/p5_global_autonomy.dart \
            lib/product/ui.dart \
            test/product/p5_command_palette_test.dart \
            test/product/source_contract_test.dart

      - name: Bootstrap hosted test dependencies
        shell: bash
        run: |
          set -euo pipefail
          python tool/v71r12_bootstrap_hosted_python.py --project .

      - name: P5-010 focused command and keyboard contracts
        shell: bash
        run: |
          set -euo pipefail
          timeout --signal=TERM --kill-after=30s 8m \
            flutter test --no-pub --concurrency=1 --reporter expanded \
            test/product/p5_command_palette_test.dart \
            test/product/p5_global_autonomy_test.dart \
            test/product/p5_main_shell_integration_test.dart \
            test/product/p5_information_architecture/p5_accessibility_test.dart \
            test/product/source_contract_test.dart

      - name: Full P5 information architecture suite
        shell: bash
        run: |
          set -euo pipefail
          timeout --signal=TERM --kill-after=30s 12m \
            flutter test --no-pub --concurrency=1 --reporter expanded \
            test/product/p5_information_architecture

      - name: Full Flutter regression suite
        shell: bash
        run: |
          set -euo pipefail
          timeout --signal=TERM --kill-after=30s 17m \
            flutter test --no-pub --concurrency=1 --reporter expanded

      - name: Fatal analyzer contract
        shell: bash
        run: |
          set -euo pipefail
          timeout --signal=TERM --kill-after=30s 8m \
            flutter analyze --no-pub --fatal-warnings --fatal-infos

      - name: Deterministic source identity and release validator
        shell: bash
        run: |
          set -euo pipefail
          python tool/p1a_refresh_source_manifest.py .
          cp SOURCE_MANIFEST.sha256 "$RUNNER_TEMP/manifest.first"
          python tool/p1a_refresh_source_manifest.py .
          cmp "$RUNNER_TEMP/manifest.first" SOURCE_MANIFEST.sha256
          python tool/p1a_text_eof_contract_test.py --project .
          python tool/validate_release.py --skip-tests
          git restore --source="$SOURCE_SHA" --staged --worktree -- \
            release/FILE_ADAPTER_RESULTS.json \
            release/KNOWLEDGE_MEMORY_V2_RESULTS.json \
            release/VALIDATION_REPORT.md \
            release/validation_report.json
          git diff --check

      - name: Assert exact six-file durable scope
        shell: bash
        run: |
          set -euo pipefail
          {
            git diff --name-only "$BASE_MAIN_SHA"
            git ls-files --others --exclude-standard
          } | sort -u > "$RUNNER_TEMP/actual.paths"
          cat > "$RUNNER_TEMP/expected.paths" <<'EOF'
          SOURCE_MANIFEST.sha256
          lib/product/p5_command_palette.dart
          lib/product/p5_global_autonomy.dart
          lib/product/ui.dart
          test/product/p5_command_palette_test.dart
          test/product/source_contract_test.dart
          EOF
          sort -o "$RUNNER_TEMP/expected.paths" "$RUNNER_TEMP/expected.paths"
          printf '%s\n' '--- P5-010 actual durable paths ---'
          cat "$RUNNER_TEMP/actual.paths"
          printf '%s\n' '--- P5-010 git status ---'
          git status --short
          diff -u "$RUNNER_TEMP/expected.paths" "$RUNNER_TEMP/actual.paths"
          test ! -e "$PATCH_PATH"
          test ! -e "$FIX_PATH"
          test ! -e "$FINALIZER_PATH"
          test -z "$(git ls-files --others --exclude-standard)"

      - name: Publish exact clean P5-010 candidate
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin main "$TARGET_BRANCH"
          test "$(git rev-parse origin/main)" = "$BASE_MAIN_SHA"
          test "$(git rev-parse origin/$TARGET_BRANCH)" = "$SOURCE_SHA"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          test -n "$(git diff --cached --name-only)"
          git commit -m 'feat(p5-010): add global command palette and keyboard system'
          git push origin "HEAD:$TARGET_BRANCH"
