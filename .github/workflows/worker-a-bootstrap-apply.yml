name: Worker A Bootstrap Apply

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  parked:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Bootstrap parked while immutable payload chunks are staged.'
