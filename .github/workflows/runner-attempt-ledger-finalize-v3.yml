name: runner-attempt-ledger-finalize-v3-placeholder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo placeholder
