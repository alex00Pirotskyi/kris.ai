# P1-012 — Create local authenticated IPC

## Status

READY

## Objective

Use named pipes/Unix sockets or loopback with mutual authentication, peer identity, request IDs, limits, and versioning.

## Dependencies

- `P1-001`
- `P1-003`

## Required output

Use named pipes/Unix sockets or loopback with mutual authentication, peer identity, request IDs, limits, and versioning.

## Acceptance criteria

- [ ] Unprivileged unrelated local process cannot invoke a worker.
- [ ] Required evidence is stored under `release/evidence/`.
- [ ] `roadmap.yaml`, STATUS, and HANDOFF are updated consistently.

## Evidence

- Pending

## Bootstrap note

This packet was materialized by P0-008 from the master roadmap. It is an execution index, not a retroactive claim that implementation or review occurred.
