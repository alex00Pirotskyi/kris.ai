# P0-004 — Pin toolchains and GitHub Actions

## Status

DONE

## Objective

Replace moving CI inputs with reviewed immutable inputs and generate one machine-readable toolchain manifest used by CI, local verification, cache keys, and release evidence.

## Dependency

- `P0-003` must be `DONE` with one green Ubuntu/Windows/macOS workflow for the same commit.

The guarded P0-004 applicator refuses to run while `release/evidence/P0-003/ci_matrix.json` is incomplete.

## In scope

1. Pin every external GitHub Action to a full 40-character commit SHA with an adjacent release-version comment.
2. Pin an exact stable Python patch version.
3. Pin an exact Flutter release; Dart is pinned transitively and recorded explicitly.
4. Replace moving runner labels with the approved release image labels.
5. Use cache keys derived from OS, architecture, exact toolchain versions, and lockfile hashes.
6. Add `config/toolchains.lock.json` and a validator.
7. Capture two reruns proving identical declared inputs.

## Out of scope

- CI topology redesign (`P9-001`).
- Dependency-vulnerability and license policy (`P8-008`).
- Installer signing or provenance (`P9`).
- Changing product behavior.

## Required inputs

The implementation session must record:

```text
checkout action release and full commit SHA
setup-python action release and full commit SHA
flutter-action release and full commit SHA
exact Python patch version
exact Flutter version
exact Dart version observed from that Flutter release
approved Ubuntu, Windows, and macOS runner labels
```

Tag names alone are not immutable and are insufficient.

## Acceptance criteria

- [ ] No `uses:` entry references a mutable tag or branch.
- [ ] No Flutter channel-only selection remains.
- [ ] Python is an exact patch version.
- [ ] Flutter and Dart versions match the toolchain manifest.
- [ ] Cache keys include exact version and lockfile identity.
- [ ] Toolchain-manifest validation passes on all three OSs.
- [ ] Two workflow reruns for the same source commit report byte-identical declared-input fingerprints.
- [ ] P0-003 remains green after pinning.

## Evidence

```text
config/toolchains.lock.json
release/evidence/P0-004/first-run.json
release/evidence/P0-004/second-run.json
release/evidence/P0-004/comparison.json
release/evidence/P0-004/review.md
```

## Reviewed Action pins for this delivery

The guarded applicator uses these immutable references, reviewed on July 23, 2026:

```text
actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
```

Exact Python, Flutter, and Dart versions are intentionally not guessed by the delivery. Supply the versions captured by the passing P0-003 workflow.

## Selected immutable inputs

```text
Python: 3.12.10
Flutter: 3.44.8
Dart: 3.12.2
Ubuntu runner: ubuntu-24.04
Windows runner: windows-2025
macOS runner: macos-15
Declared-input fingerprint: 532f73ad316f6887ce1fdf3f7252ee371ad1d4e98062b1e3e3a9673c1da64818
```

P0-004 remains REVIEW until two same-source workflow reruns compare equal on all three lanes.

## P0 exit closure

- `release/evidence/P0/P0_EXIT_GATE_V44.json`
