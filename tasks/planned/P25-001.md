# P25-001 — Instrument and benchmark the real Prompt Studio path

## Status

`READY`

## Dependencies

- none

## Objective

Measure the complete prompt and plan path without changing semantics.

## Required implementation


1. Instrument UI dispatch, model discovery, warm-up, request opening, first token, generation, parsing, validation, repair, persistence, and completion.
2. Emit ordered, secret-minimized trace records with exact model and token metadata.
3. Add deterministic unit evidence and a real owner-machine benchmark harness.
4. Do not change prompt or plan semantics in this task.


## Test Station

- Run the cases mapped to `P25-001` in `docs/roadmap/p25/prompt_studio_test_station.v1.json`.
- Preserve exact assurance classes.
- Missing implementation or environment is blocked, never passed.

## Acceptance

- Exact candidate tests pass.
- Evidence binds exact commit/tree and does not mutate tracked source.
- No valid test or performance budget is weakened.
- Hidden chain-of-thought is not exposed.
- No packaged/platform/release/production/GA claim is inferred from source evidence.

## Evidence

- `release/evidence/P25-001/`
- exact GitHub Actions run
- independent exact-candidate review
