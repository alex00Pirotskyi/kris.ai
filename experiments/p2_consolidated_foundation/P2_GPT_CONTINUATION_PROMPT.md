# New-chat prompt — continue Kristin P2 from the consolidated foundation

You are continuing TRAIN-02 / P2 for the native local-first Kristin agent.

You will receive:

1. the repository source snapshot from branch `integration/p2-consolidated-foundation-v1` repaired by consolidated foundation V2;
2. `KRISTIN_P2_CONSOLIDATED_SOURCE_FOUNDATION_V1.zip` or the applied experiment directory;
3. this handoff.

Read the Markdown/JSON files under `experiments/p2_consolidated_foundation/` first. The complete V62 reference source is sealed at `reference_archives/KRISTIN_P2_V62_REFERENCE_SOURCE.zip`. Extract it only into a disposable directory outside the governed repository; do not commit the extracted tree under `experiments/`. The repository snapshot is authoritative for current APIs. The task-reduction matrix is authoritative for what is already available and what remains.

## Non-negotiable rules

- Do not rebuild the fourteen P2 tasks independently.
- Work through the five remaining workstreams W1–W5.
- Reuse and repair the V62 delegation-only reference overlay.
- Do not copy rejected authority-service or evidence-finalization patterns from earlier versions.
- P2 must consume a merged, isolated P1A service; P2 must not define the P1 TCB.
- The worker must run under an OS-enforced restricted principal distinct from the desktop authority.
- No source-only, fixture-only, helper-only or descriptive evidence can mark a task complete.
- Do not create `tasks/completed/P2-xxx.md` until exact task-specific production evidence passes.
- Preserve P1 evidence and protected-main history.
- Use separate source and evidence commits, exact-SHA resumability, independent review, required PR checks and merged-main tri-OS verification.

## First actions

1. Record exact repository base commit/tree, branch ancestry, toolchain locks and workflows.
2. Run P1 exit gates and determine whether P1A is merged and valid.
3. Extract the sealed reference archive outside the repository and compare live source paths to its `p2_v62/overlay` tree.
4. Produce a port/reuse matrix: unchanged, repair, replace, discard.
5. Implement W1 first, because W2–W5 cannot close without the authority/worker boundary.
6. Continue until the maximum safely provable result is packaged.

## Required final report

Report:

- exact source/evidence commits and trees;
- which of W1–W5 are complete;
- P2-001 through P2-014 machine-evidence status;
- exact platform receipts and workflow runs;
- independent review and owner approval bindings;
- protected-main merged SHA and exact merged-main workflows;
- remaining blockers without overclaiming.
