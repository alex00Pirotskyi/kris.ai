# Browser task

Open `index.html`, fill the name field with `Kristin`, submit the form, and return JSON proving that the receipt `KRISTIN-FIXTURE-OK` became visible. Include before and after observation evidence. This case must remain `unsupported` until a real browser capability exists.
