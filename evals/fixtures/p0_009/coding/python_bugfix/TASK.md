# Coding task — repair division

The `divide` function returns the wrong result. Repair the smallest possible implementation without changing the public API. Preserve the explicit zero-division error and make the supplied tests pass.
