# Analysis task

Read the two local evidence files. Return a JSON object with `summary.currentAuthority`, `summary.v1Trust`, a `claims` array, and a `citations` array. Distinguish runtime behavior from source-only claims.
