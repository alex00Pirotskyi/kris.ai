# Runtime boundary evidence

The current implementation uses the durable SQLite workflow kernel as its authoritative local workflow state. Model output proposes actions; it does not grant authority. Owner Mode is a future explicit full-host policy and must not be described as a sandbox.
